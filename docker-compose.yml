version: '3.8'

services:
  api:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env
    restart: always

  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env
    depends_on:
      - api
    restart: always 